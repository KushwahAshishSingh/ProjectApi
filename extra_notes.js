import { Router } from "express";

// use of next for blocking loop in postman 

// use var data = JSON.parse(this.anyBody);

// res.send()  allow us to send any type of response like html json etc (<html>hello</html>)
// use capital letter fr a class

// const occup = req.body.ocup;   use same in json file


// this.id = Math.random().toString();   in userLogin Model for adding an id to the product


// Router.get('/product/:productID' )  means for every request localhost:3000/product/2323 it will work fine bcz of :